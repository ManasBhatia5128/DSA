package Arrays.Easy;

public class NumberAppearOnce {
    // Brute: Linear search for all elem O(N2)
    // Better: Hashing
    // Optimal: use xor operator all over elements of array, common elements cancel each other
}
